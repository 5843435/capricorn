<% require 'rubygems' %>
<% require 'active_support' %>
<%- model_class = Stock -%>
<div class="page-header">
  <h3>マイページ</h3>
</div>
<% if @stocks.empty? %>
  <h5><img height="80" src="/assets/icons/panda0.png">在庫登録がないよ。下のボタンから登録してね。</h5>
<% else %>
<% if @week1.present? %>
  <h5><img height="80" src="/assets/icons/panda1.png">あと１週間以内になくなりそうだよ、買いに行かなきゃ</h5>
<% elsif @week2.present? %>
  <h5><img height="80" src="/assets/icons/panda2.png">あと１週間以上は大丈夫だよ。そろそろ買い足しといた方がいいかも</h5>
<% elsif @week3.present? %>
  <h5><img height="80" src="/assets/icons/panda3.png">あと２週間以上は大丈夫だよ。買い足し時期はいつごろかな？</h5>
<% elsif @week4.present? %>
  <h5><img height="80" src="/assets/icons/panda4.png">あと３週間以上は大丈夫だよ。買い足し時期はいつごろかな？</h5>
<% else %>
  <h5><img height="80" src="/assets/icons/panda5.png">あと１か月くらいは大丈夫だよ。買い足し時期はいつごろかな？</h5>
<% end %>
<table class="table table-striped table-bordered table-hover table-condensed">
    <tbody>
     <tr>
       <td>買い足しどき</td>
       <td>商品</td>
     </tr>
     <tr>
        <td>1週間以内 急いで買わなきゃ！</td>
        <td><% @week1.each do |key, value| %>
          <img width="30" height="30" src="/assets/icons/<%= key %>.png">
          <%= value %><% end %></td>
      </tr>
      <tr>
        <td>2週間以内 そろそろ買っとく？</td>
        <td><% @week2.each do |key, value| %>
          <img width="30" height="30" src="/assets/icons/<%= key %>.png">
          <%= value %><% end %></td>
      </tr>
      <tr>
        <td>3週間以内 まぁ大丈夫でしょ</td>
        <td><% @week3.each do |key, value| %>
          <img width="30" height="30" src="/assets/icons/<%= key %>.png">
          <%= value %><% end %></td>
      </tr>
      <tr>
        <td>4週間以内 まだまだ余裕〜</td>
        <td><% @week4.each do |key, value| %>
          <img width="30" height="30" src="/assets/icons/<%= key %>.png">
          <%= value %><% end %></td>
      </tr>
      <tr>
        <td>たくさんあるよー</td>
        <td><% @week5.each do |key, value| %>
          <img width="30" height="30" src="/assets/icons/<%= key %>.png">
          <%= value %><% end %></td>
      </tr> 
    </tbody>
</table>
<table class="table table-striped">
  <thead>
    <tr>
      <th>商品名</th>
      <th>登録日</th>
      <th>なくなりそうな日</th>
      <th>あとこれくらいあるかも</th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @stocks.each do |stock| %>
      <% end_day = calcEndday(stock) %> 
       <% remain_day = (( end_day - Time.now ) / ( 60 * 60 * 24 )).truncate %>
       <% if (( end_day - Time.now ) / ( 60 * 60 * 24 )).truncate < 0 %><% next %><% end %>
      <tr>
        <td><%= link_to stock.item.name, stock if stock.item %></td>
        <td><%= stock.updated_at.strftime("%Y年%m月%d日") %></td>
        <td><%= end_day.year %>年<%= end_day.month %>月<%= end_day.day %>日
          （あと<%= remain_day %>日）</td>
            <% remain_num = stock.num * (end_day - Time.now) / (end_day - stock.updated_at) %>
              <td>
            <% if remain_num >= 1 %>
          　  <% instance_div = remain_num.floor.div(10) %>
              <% instance_div.times { %>
                <img width="40" height="40" src="/assets/icons/<%= stock.item.id %>_10.png">
                <% } %>
                <% instance_modulo = remain_num.floor.modulo(10) %>
                <% instance_modulo.times { %>
                <img width="30" height="30" src="/assets/icons/<%= stock.item.id %>.png">
                <% } %>
                <%= remain_num.floor %>個
            <% else %>
                <% if remain_num < 0 %>
                <img width="30" height="30" src="/assets/icons/<%= stock.item.id %>.png">
                  0%
                <% else %>
                <img width="30" height="30" src="/assets/icons/<%= stock.item.id %>.png">
                  <%= (remain_num * 100).to_i %>%
                <% end %>
            <% end %>
  　　　　    </td>
        <td>
          <%= link_to t('.increase_day', :default => t("今日は使ってないよ")),
                      stock_path(stock),
                      #:href => "/stocks/increace_day/#{stock.id}",
                      :method => :put,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'なくなる日を１日増やしますか？')) },
                      :class => 'btn btn-xs btn-success' %>
          <%= link_to t('.destroy', :default => t("もう無くなったよ")),
                      stock_path(stock),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => '本当に削除しますか？')) },
                      :class => 'btn btn-xs btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
※商品を削除するときは「もうなくなったよ」ボタンを押してね<br>
※商品を使用していないときや、なくなる日の調整を行いたいときは「今日は使ってないよ」ボタンを押してね<br>
<br>
<% end %>
<%= link_to t('.new', :default => t("helpers.links.new")),
            new_stock_path,
            :class => 'btn btn-primary' %>

<%= link_to '過去の履歴を見る', search_stocks_path, :class => 'btn btn-info' %>